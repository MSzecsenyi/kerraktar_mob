import { StyleSheet, Text, View } from "react-native";

const TakeOutAcceptListItem = () => {
	return (
		<View>
			<Text>TakeOutAcceptListItem</Text>
		</View>
	);
};

export default TakeOutAcceptListItem;

const styles = StyleSheet.create({});
